import { ImagenPeliculaPipe } from './imagen-pelicula.pipe';

describe('ImagenPeliculaPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenPeliculaPipe();
    expect(pipe).toBeTruthy();
  });
});
